services:
  - type: web
    name: network-ids-portal
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
